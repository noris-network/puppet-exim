---
exim::defaults: true

exim::manage_service: true

exim::retries:
  'ensureresource': {}

exim::rewrites:
  'ensureresource':
    pattern: 'test@example.com'
    replacement: '*'
    flags:
      - 'f'
